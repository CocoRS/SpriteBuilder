checkout:
    post:
        - git submodule update --init --recursive

dependencies:
    override:
        - rake package
test:
    override:
        - rake test


general:
    branches:
        only:
            - master
            - develop
            - experimental/circleciSign
    artifacts:
        - Build/SpriteBuilder.app
        - Build/SpriteBuilder.app.dSYM
        - Build/SpriteBuilder.xcarchive

